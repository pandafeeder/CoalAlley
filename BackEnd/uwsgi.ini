[uwsgi]

# Django-related settings
# the base directory (full path)
chdir           = /Users/qusr/GitHub/learnDjango/CoalAlley/BackEnd
# Django's wsgi file
module          = CoalAlley.wsgi
# the virtualenv (full path)
home            = /Users/qusr/GitHub/learnDjango/ldbe

# process-related settings
# master
master          = true
# maximum number of worker processes
processes       = 2
# the socket (use the full path to be safe
socket          = /Users/qusr/GitHub/learnDjango/CoalAlley/BackEnd/CoalAlley.sock
# ... with appropriate permissions - may be needed
# chmod-socket    = 664
# clear environment on exit
vacuum          = true
